# Java 贪心检查框架

贪心不是“看起来最优就选”。必须能说明：

1. 当前选择不会破坏最优解；
2. 丢掉的选择不可能带来更优答案；
3. 维护的状态能概括过去全部信息。

常见状态：

```java
int farthest = 0;
for (int i = 0; i <= farthest && i < nums.length; i++) {
    farthest = Math.max(farthest, i + nums[i]);
}
```
