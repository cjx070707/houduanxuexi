# Java 滑动窗口模板

```java
int left = 0;
for (int right = 0; right < s.length(); right++) {
    // 1. 把 right 加入窗口

    while (/* 窗口不合法，或可以继续收缩 */) {
        // 2. 移除 left
        left++;
    }

    // 3. 用当前合法窗口更新答案
}
```

不要背代码，先说清楚：窗口维护什么、什么时候不合法、答案在收缩前还是收缩后更新。
