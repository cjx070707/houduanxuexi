controller       接收前端请求
service          业务接口
service/impl     真正的业务代码，学习重点
mapper           操作 MySQL
entity           数据库表对应的 Java 对象
dto              前后端传输的数据
utils            Redis、登录、锁等工具
config           Spring 配置
resources        yaml、SQL、Lua 脚本

前端请求 ↓ Controller ↓ ServiceImpl ↓ Mapper / Redis ↓ MySQL / Redis



## 第一阶段：普通 CRUD，建立基本链路

先看店铺查询，不要先看秒杀。

阅读顺序：

```
ShopController
→ IShopService
→ ShopServiceImpl
→ ShopMapper
→ Shop
→ Result
```
